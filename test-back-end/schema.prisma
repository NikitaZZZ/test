datasource db {
  provider = "postgresql"
  url      = env("postgres://postgres:postgrespw@localhost:55002")
}

generator client {
  provider = "prisma-client-js"
}

model Dish {
  id Int @id @default(autoincrement())
  title String
  category String
  compound String
}